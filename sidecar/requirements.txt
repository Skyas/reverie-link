# ── 核心后端 ──────────────────────────────────────────────────
fastapi
uvicorn[standard]
openai
python-dotenv
httpx
websocket

# ── Phase 3: 记忆系统 ─────────────────────────────────────────
chromadb

# ── Phase 3: 视觉感知 ─────────────────────────────────────────
Pillow
psutil

# ── Phase 2: 本地语音合成 ─────────────────────────────────────
# edge-tts：本地 TTS 底层引擎（替代原 pyttsx3 方案，调用微软 Edge 在线合成）
edge-tts
# 以下为本地 RVC v2 变声依赖（rvc_infer.py 实装后启用）
# torch
# rvc-python

# ── Phase 4: 语音输入（STT）────────────────────────────────────
# sherpa-onnx：本地 SenseVoice 语音识别引擎
# 安装后需下载模型到 sidecar/models/sense-voice-small-int8/
# 若未安装，语音输入模块会自动降级为不可用状态
sherpa-onnx
